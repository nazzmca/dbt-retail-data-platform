version: 2

sources:
  - name: retail_raw
    database: dev
    schema: main
    description: "Raw retail order data"

    tables:
      - name: raw_orders
        description: "Raw orders loaded into DuckDB"